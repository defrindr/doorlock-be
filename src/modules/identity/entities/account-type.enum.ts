export enum AccountType {
  EMPLOYEE = 'employee',
  INTERN = 'intern',
  GUEST = 'guest',
}

export enum IdentificationType {
  KTP = 'ktp',
  PASSPORT = 'passport',
  DRIVER_LICENSE = 'driver_license',
  OTHER = 'other',
}
